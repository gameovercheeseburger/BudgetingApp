spring.application.name=budgetingapp-backend

# Server port
server.port=8080

# Enable CORS globally (alternative to @CrossOrigin)
# spring.mvc.cors.allowed-origins=http://localhost:3000

# Database config (optional)
# spring.datasource.url=jdbc:h2:mem:testdb
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=

## ===== Database Configuration for Local MSSQL =====
spring.datasource.url=jdbc:sqlserver://localhost:1433;databaseName=BudgetingApp;encrypt=true;trustServerCertificate=true
spring.datasource.username=sa
spring.datasource.password=Password123

# MSSQL JDBC Driver
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# Hibernate Configuration (JPA)
spring.jpa.database-platform=org.hibernate.dialect.SQLServerDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Connection Pool Settings (Optional)
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.max-lifetime=1800000
